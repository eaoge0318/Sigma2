# Y 軸點擊調整功能 - 實現完成

## 🎉 功能說明

**點擊圖表左側的 Y 軸區域**,會彈出一個模態框讓您調整 Y 軸範圍!

### 使用方式:

1. **點擊 Y 軸**: 點擊圖表左側的 Y 軸區域 (目標變數名稱附近)
2. **選擇模式**:
   - 🔄 **自動範圍**: 根據 LSL/USL 自動計算 (預設)
   - ✏️ **手動範圍**: 自己設定最小值和最大值
3. **調整數值** (手動模式):
   - 輸入最小值
   - 輸入最大值
4. **套用**: 點擊"套用"按鈕,所有圖表立即更新

---

## ✅ 已完成的修改

### 1. JavaScript 全域變數
```javascript
let yAxisMode = 'auto';  // 'auto' 或 'manual'
let yAxisManualMin = null;
let yAxisManualMax = null;
```

### 2. createChart 函數
- 加入 `onClick` 事件處理
- 檢測點擊 Y 軸區域 (左側 0-60px)
- 點擊時調用 `showYAxisRangeModal()`

### 3. 模態框函數
- `showYAxisRangeModal()` - 顯示調整模態框
- `closeYAxisRangeModal()` - 關閉模態框
- `updateYAxisModeInModal()` - 更新模式選擇
- `updateCurrentRangeDisplay()` - 顯示當前範圍
- `applyYAxisRangeFromModal()` - 套用設定
- `recreateAllCharts()` - 重建所有圖表

---

## 🎨 模態框 UI

```
┌─────────────────────────────────────┐
│  Y 軸範圍設定                        │
├─────────────────────────────────────┤
│  ○ 自動範圍                          │
│  ○ 手動範圍                          │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ 最小值: [1.50]              │   │
│  │ 最大值: [2.50]              │   │
│  └─────────────────────────────┘   │
│                                     │
│  當前範圍: 1.54 ~ 2.44              │
│                                     │
│              [取消]  [套用]         │
└─────────────────────────────────────┘
```

---

## 🔍 技術細節

### 點擊檢測
```javascript
onClick: (event, activeElements, chart) => {
    const canvasPosition = Chart.helpers.getRelativePosition(event, chart);
    
    const yAxisArea = {
        left: 0,
        right: 60,  // Y 軸標題寬度
        top: chart.chartArea.top,
        bottom: chart.chartArea.bottom
    };
    
    if (canvasPosition.x >= yAxisArea.left && canvasPosition.x <= yAxisArea.right &&
        canvasPosition.y >= yAxisArea.top && canvasPosition.y <= yAxisArea.bottom) {
        showYAxisRangeModal(actionName);
    }
}
```

### 範圍計算
```javascript
let yMin, yMax;
if (yAxisMode === 'manual' && yAxisManualMin !== null && yAxisManualMax !== null) {
    yMin = yAxisManualMin;
    yMax = yAxisManualMax;
} else {
    yMin = deadzone[0] * 0.9;  // LSL - 10%
    yMax = deadzone[1] * 1.1;  // USL + 10%
}
```

---

## 📋 測試步驟

1. **重新整理頁面**
2. **執行模擬**,確保圖表顯示
3. **點擊 Y 軸** (左側的目標變數名稱附近)
4. **模態框應該彈出**
5. **選擇"手動範圍"**
6. **輸入數值** (例如: 1.5 ~ 2.5)
7. **點擊"套用"**
8. **圖表應該更新**,Y 軸範圍變為 1.5-2.5
9. **再次點擊 Y 軸**
10. **選擇"自動範圍"**
11. **點擊"套用"**
12. **圖表恢復自動範圍**

---

## ⚠️ 注意事項

1. **點擊區域**: 只有點擊圖表左側 0-60px 的區域才會彈出模態框
2. **數值驗證**: 系統會檢查輸入的數值是否有效
3. **最小值 < 最大值**: 系統會確保最小值小於最大值
4. **即時更新**: 套用後所有圖表立即重新繪製
5. **模式保留**: 手動模式下的設定會保留,直到切換回自動模式

---

## 🎯 優點

✅ **直觀**: 點擊 Y 軸即可調整,不需要額外的 UI 按鈕
✅ **靈活**: 支援自動和手動兩種模式
✅ **即時**: 套用後立即看到效果
✅ **簡潔**: 模態框設計簡潔,易於使用
✅ **智能**: 自動模式根據 LSL/USL 計算合理範圍

---

## 📝 總結

✅ **所有功能已實現**
✅ **無需額外的 HTML UI**
✅ **點擊 Y 軸即可調整**

現在請**重新整理頁面並測試**!

點擊圖表左側的 Y 軸區域,應該會彈出調整模態框。
